apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: netbird-dashboard
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    name: netbird-dashboard
spec:
  project: default
  source:
    repoURL: https://charts.jaconi.io
    targetRevision: 1.2.0
    chart: netbird-dashboard
    helm:
      valuesObject:
        auth:
          autority: https://auth.adryan.me/realms/master
          audience: netbird-dashboard
          clientId: netbird-dashboard
        netbird:
          managementApiEndpoint: http://netbird-management.netbird.svc.cluster.local:80/api
          managementGrpcApiEndpoint: http://netbird-management.netbird.svc.cluster.local:80/management.ManagementService/


  destination:
    server: https://kubernetes.default.svc
    namespace: netbird

  syncPolicy:
    managedNamespaceMetadata:
      labels:
        istio-injection: enabled
    automated:
      enabled: true
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - RespectIgnoreDifferences=true

    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 3
