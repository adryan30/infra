apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: calibre
  namespace: istio-ingress
  labels:
    app: calibre
spec:
  hosts:
    - "calibre.adryan.me"
  gateways:
    - public
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            port:
              number: 8083
            host: calibre.books.svc.cluster.local
