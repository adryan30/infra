apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openvpn-persistance
  namespace: openvpn
spec:
  resources:
    requests:
      storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
