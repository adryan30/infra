apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: zurg-config
  namespace: streaming
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  target:
    name: zurg-config
  dataFrom:
    - extract:
        key: streaming/zurg
