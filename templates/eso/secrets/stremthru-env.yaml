apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: stremthru-env
  namespace: streaming
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  target:
    name: stremthru-env
  dataFrom:
    - extract:
        key: streaming/stremthru
